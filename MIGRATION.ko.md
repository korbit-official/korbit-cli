# `korbit`에서 `dgx-cli`로 업그레이드하기

[English](MIGRATION.md) · **한국어**

**v1.2.0**에서 CLI의 이름이 바뀝니다. 명령 `korbit` → `dgx-cli`, 홈 `~/.korbit-cli` →
`~/.digitalx-cli`, 변수 `KORBIT_CLI_*` → `DIGITALX_CLI_*`. **고장 나는 것도, 옮겨지는 것도
없습니다. 전부 그대로 동작합니다.** 이전 이름은 모두 기한 없이 계속 인식되며, 이 CLI의 어떤
명령도 디스크에 있는 것을 옮기거나 이름을 바꾸지 않습니다.

## 나에게 해당되나요?

| 이런 경우라면 | 할 일 |
|---|---|
| `dgx-cli`를 새로 설치했다 | 없습니다. 이 문서는 읽지 않아도 됩니다. |
| `korbit` 설치본이 있다 | `korbit self update`를 **두 번** 실행하세요([왜 두 번인지](#self-update를-두-번-실행하세요)). 데이터는 있던 자리에서 그대로 동작합니다. |
| `KORBIT_CLI_*` 변수를 설정해 두었다 | 없습니다. 계속 인식되며, 둘 다 설정되어 있으면 `DIGITALX_CLI_*`가 이깁니다. |
| `.mcpb` 데스크톱 확장을 설치했다 | 이전 확장을 직접 제거하세요 — 새 확장은 그 [옆에 설치됩니다](#mcpb-번들은-별개의-확장으로-나타납니다). |
| 홈을 `~/.digitalx-cli`로 바꾸고 싶다 | 선택 사항이며 직접 해야 합니다 — [두 가지 방법](#새-디렉터리-이름으로-옮기기-선택). |

## 바뀐 것

| 항목 | 현재 | 이전 | 이전 이름은… |
|---|---|---|---|
| 명령 | `dgx-cli` | `korbit` | 그 이름이 있는 설치본에서는 같은 바이너리의 **별칭**(alias)으로 영구 유지. 새 설치에는 `dgx-cli`만 |
| CLI 홈 | `~/.digitalx-cli` | `~/.korbit-cli` | 있는 그대로 영구 사용. 옮기지 않음 |
| 홈 변수 | `DIGITALX_CLI_HOME` | `KORBIT_CLI_HOME` | 계속 인식. 둘 다 설정되면 현재 이름 우선 |
| 아티팩트 캐시 | `<사용자 캐시 디렉터리>/digitalx-cli` | `<사용자 캐시 디렉터리>/korbit-cli` | 있는 그대로 영구 사용. 옮기지 않음 |
| 캐시 변수 | `DIGITALX_CLI_SANDBOX_CACHE` | `KORBIT_CLI_SANDBOX_CACHE` | 계속 인식. 규칙 동일 |
| 그 밖의 모든 변수 | `DIGITALX_CLI_*` | `KORBIT_CLI_*` | 계속 인식. 규칙 동일 |
| 릴리스 아카이브 | `digitalx-cli_<os>_<arch>` | `korbit_<os>_<arch>` | `korbit` 설치본이 업데이트하며 거쳐 가는 릴리스에 한 번만 게시. 이후 릴리스는 현재 이름만 |

## `korbit` 설치본 업그레이드

### `self update`를 두 번 실행하세요

```sh
korbit self update    # 1. 현재 릴리스를 설치합니다 — 아직 `korbit` 이름 그대로입니다
korbit self update    # 2. `dgx-cli` 명령을 추가하고 `korbit`이 그것을 가리키게 합니다
```

**두 번인 이유:** `dgx-cli` 명령을 만드는 코드가 첫 번째 실행이 내려받는 릴리스 안에 들어
있기 때문입니다. 그래서 첫 번째 실행을 마치면 최신이 되지만 여전히 `korbit` 하나뿐이고, 두
번째 실행이 바로 그 새 코드로서 `dgx-cli`를 추가하며, 고친 내용을 `layoutRepaired`에 담아
`updated: false`로 보고합니다 — 업데이트된 것은 없고 정비만 되었다는 뜻입니다.

그다음부터는 **두 명령 이름이 모두 최신으로 유지됩니다.** 모든 설치와 업데이트가 `dgx-cli`를
실제 바이너리로 놓고, 그 옆에 같은 바이트를 가리키는 별칭으로 `korbit`을 유지합니다. 어느
이름으로 실행하든 같은 CLI입니다.

### Agent Skill은 설치할 때 이름이 바뀝니다

`dgx-cli agent skill install`은 스킬을 현재 디렉터리 이름으로 쓰고 이전 이름으로 남아 있는
사본을 제거하므로, 에이전트에게는 스킬이 둘이 아니라 하나로 보입니다. 이 이름 변경은
에이전트 자신의 스킬 폴더 안에서만 일어나며, CLI 홈은 건드리지 않습니다.

### `.mcpb` 번들은 별개의 확장으로 나타납니다

데스크톱 확장(Desktop Extension)은 매니페스트에 적힌 이름을 키로 삼으므로, 현재 이름으로
빌드된 번들은 이미 있는 것을 대체하지 않고 그 **옆에** 설치됩니다 — 항목 둘, 서버 둘, 모든
도구의 사본 둘. 확장에는 자체 업데이트 경로가 없으니, 이전 것은 호스트의 확장 목록에서
직접 제거하세요.

## 어떤 명령도 데이터를 옮기지 않습니다

**디스크에 있는 것은 무엇도 대신 옮겨 주거나 이름을 바꿔 주지 않습니다** — 홈도, 캐시도, 그
안의 파일도 — 그리고 그렇게 해 달라고 요청하는 플래그도 없습니다. 이전 이름의 홈은 그
자리에 두는 한 그냥 그대로 홈입니다.

| 명령 | 하는 일 | 절대 하지 않는 일 |
|---|---|---|
| `self install` (설치 한 줄 명령이 실행하는 것) | 바이너리 배치, `PATH` 연결, 매니페스트 기록. 다시 실행하면 망가진 설치를 고치는 것 외에는 아무것도 하지 않음 | 디렉터리 이동, 홈 안의 파일 이름 변경, 사용자 데이터 삭제 |
| `self update` | 릴리스 내려받기, 바이너리 제자리 교체, 명령 배치 유지(`dgx-cli`가 실제, `korbit`이 그것을 가리킴) | 디렉터리 이동, 파일 이름 변경 |
| `self doctor` | 보고만 — 계약상 읽기 전용 | 어떤 변경도 |
| `self uninstall` | 확인한 것만 삭제([상세](#방법-a--처음부터-다시-제거-후-재설치)) | 이름 변경, 이동 |

## CLI가 홈과 캐시를 찾는 곳

### CLI 홈

순서대로, 먼저 맞는 것이 이깁니다.

1. `$DIGITALX_CLI_HOME` — 설정되어 있을 때
2. `$KORBIT_CLI_HOME` — 설정되어 있을 때
3. `~/.digitalx-cli` — 그 디렉터리가 있을 때
4. `~/.korbit-cli` — 그 디렉터리가 있을 때
5. `~/.digitalx-cli` — 처음 쓸 때 생성

3번과 4번 때문에, **두** 디렉터리가 모두 있으면 CLI는 `~/.digitalx-cli`를 읽고 다른 하나는
보이지 않습니다. `self doctor`가 그 상태를 [보고합니다](#self-doctor가-보고하는-것).

### 아티팩트 캐시

형태는 같고, 먼저 맞는 것이 이깁니다.

1. `$DIGITALX_CLI_SANDBOX_CACHE` — 설정되어 있을 때
2. `$KORBIT_CLI_SANDBOX_CACHE` — 설정되어 있을 때
3. `<사용자 캐시 디렉터리>/digitalx-cli` — 있을 때
4. `<사용자 캐시 디렉터리>/korbit-cli` — 있을 때
5. `<사용자 캐시 디렉터리>/digitalx-cli`

캐시에는 언제든 다시 만들 수 있는 내려받기 결과(관리되는 Deno 런타임과 그 모듈 캐시)만
들어 있으므로, 어느 쪽을 쓰든 차이가 없습니다.

### CLI 홈 안의 파일

홈 안의 파일에는 **제품 이름이 들어가지 않습니다** — 디렉터리가 이미 누구의 데이터인지 말해
주기 때문입니다.

| 홈 기준 경로 | 무엇인지 | 동반 파일 |
|---|---|---|
| `journal.db` | 작업 저널 | `-wal`, `-shm` |
| `bot.db` | `monitor` 봇 런타임의 스크립트 전용 데이터베이스 | `-wal`, `-shm` |
| `sandbox/sandbox.db` | 로컬 API 샌드박스의 데이터베이스 | `-wal`, `-shm`, `-pid`, `.market-snapshot.json` |
| `debug-<timestamp>.json` | `debug bundle`이 만든 진단 번들 | — |
| `config.json`, `keys.json`, `keystore.json`, `install.json`, `self.lock`, `sandbox/run.log` | 설정, 키, 관리 정보 | — |

**예외는 하나, 규칙도 하나입니다. 디렉터리 이름 자체가 `.korbit-cli`인 홈에서는 각
데이터베이스가 이전 이름을 그대로 씁니다.**

| `.korbit-cli`라는 이름의 홈에서 | 그 밖의 모든 홈에서 |
|---|---|
| `korbit-cli.db` (+ `-wal`, `-shm`) | `journal.db` (+ `-wal`, `-shm`) |
| `korbit-bot.db` (+ `-wal`, `-shm`) | `bot.db` (+ `-wal`, `-shm`) |
| `sandbox/korbit-sandbox.db` (+ `-wal`, `-shm`, `-pid`, `.market-snapshot.json`) | `sandbox/sandbox.db` (+ `-wal`, `-shm`, `-pid`, `.market-snapshot.json`) |

- **이유:** `~/.korbit-cli`를 여전히 함께 쓰는 이전 `korbit` 바이너리와 현재 바이너리가 같은
  파일을 읽고 씁니다 — 둘 다 같은 디렉터리에서 같은 이름을 이끌어내기 때문입니다. CLI의
  다른 어떤 부분도 이전 이름을 참조하지 않습니다.
- **이름의 기준:** 절대 경로로 해석한 뒤의 디렉터리 **자기** basename입니다. 상대 경로로
  접근한 홈이나, 대소문자를 구분하지 않는 파일 시스템에서 `~/.KORBIT-CLI`로 적은 홈도 같은
  파일 이름을 쓰는 같은 홈이지, 같은 디렉터리 안의 또 다른 배치가 아닙니다.
- **고정된 홈도 이 규칙을 따릅니다.** `DIGITALX_CLI_HOME`이나 `KORBIT_CLI_HOME`으로
  `.korbit-cli`라는 이름의 디렉터리에 고정하면 이전 이름을, 그 밖의 곳에 고정하면 현재
  이름을 씁니다.
- **디렉터리 이름과 파일 이름은 한 묶음입니다.** 파일은 그대로 두고 디렉터리만 이름을
  바꾸면, 내용이 가득 찬 홈을 CLI가 비어 있다고 읽습니다 — 진짜 데이터 옆에 비어 있는
  데이터베이스를 새로 열게 됩니다. 그래서 [수동 절차](#방법-b--직접-옮기기)는 둘을 한 번에
  처리하고, `self doctor`는 반쯤 끝난 이 상태를 문제로 보고합니다.

## 새 디렉터리 이름으로 옮기기 (선택)

굳이 할 필요는 없습니다. 하고 싶다면 두 가지 방법이 있습니다.

### 방법 A — 처음부터 다시: 제거 후 재설치

데이터를 버리고 CLI를 맨 처음부터 다시 설정합니다. 홈에 남겨 둘 만한 것이 없을 때(또는
필요한 것을 이미 내보내 두었을 때) 선택하세요.

```sh
dgx-cli self uninstall     # 대화형입니다. 정확히 무엇을 하는지는 아래 참고
# 그다음 설치 한 줄 명령을 다시 실행하고:
dgx-cli setup
```

`self uninstall`은 **대화형 전용**입니다. 터미널이 필요하며, 파이프나 리다이렉션으로
연결되었을 때, 에이전트에서 실행되었을 때, `--json` / `--compact`와 함께일 때는 실행을
거부합니다. 자체 플래그는 없고, 전역 `--dry-run`은 같은 질문을 던지되 아무것도 바꾸지 않으며
답변대로 했다면 무엇이 일어났을지만 알려 줍니다.

**세 가지 질문**을 따로따로 합니다. 답하기 전에는 아무것도 건드리지 않으며, **아니오**라고
답하면 그 행의 것은 전부 남습니다.

| 질문 | 기본값 | 예라고 답하면 제거 | 어떻게 답해도 유지 |
|---|---|---|---|
| `dgx-cli` 바이너리와 설치 매니페스트 | **예** | 바이너리, 이 설치본이 소유한 모든 별칭(`korbit` 포함), `install.json`, 남아 있는 교체용 임시 파일 | 자기 것임을 증명할 수 없는 `korbit` 이름의 파일 |
| 설정, API 키, 작업 저널 | **아니오** | `config.json`, `journal.db`/`korbit-cli.db`와 `bot.db`/`korbit-bot.db`(각각의 `-wal`/`-shm` 포함), `debug-*.json`, `keys.json`과 `keystore.json`(각 키를 OS 키체인을 포함한 해당 백엔드에서 먼저 지운 뒤) | — |
| 다시 만들 수 있는 캐시 | **아니오** | 각 홈의 `sandbox/`, **두** 캐시 이름 아래의 공유 Deno/모듈 캐시(실행 중인 샌드박스는 먼저 중지) | — |

그다음, 설치 프로그램의 `PATH` 블록이 들어 있는 셸 시작 파일마다 차이(diff)를 보여 주고
**그 블록만** 제거할지 묻습니다(기본값 **아니오**) — 파일 자체는 결코 지우지 않습니다.
Windows에서는 사용자 `PATH`에서 digitalx-cli 항목만 제거합니다. 거절한 파일은 직접 편집할
수 있도록 알려 줍니다.

알아 둘 점:

- **모든 홈이 대상입니다** — 사용 중인 홈만이 아니라, `~/.digitalx-cli`와 `~/.korbit-cli`가
  있으면 그 각각, 그리고 고정된 홈까지. 각 홈의 데이터베이스는 **두** 표기 모두로
  나열되므로, 파일은 두고 디렉터리만 이름을 바꾼 홈도 남김없이 정리됩니다.
- **홈 디렉터리 자체는 비었을 때만 제거됩니다.** 데이터나 캐시를 남겼거나 제거가
  실패했으면 디렉터리는 그대로 남습니다 — 실패가 아니라 의도적으로 남긴 것입니다.
- **키 제거는 되돌릴 수 없습니다.** 키 파일을 지우기 전에 각 키를 해당 백엔드(OS 키체인
  포함)에서 먼저 지우므로 뒤에 남는 항목은 없습니다 — 다만 키는 사라집니다.
- **Windows에서는 실행 중인 `.exe`가 잠겨 있어** 스스로를 지울 수 없으므로, 직접 지울 수
  있도록 알려 줍니다.

### 방법 B — 직접 옮기기

전부 그대로 보존합니다. 네 단계를 순서대로 진행하세요.

**1. 그 홈을 쓰는 모든 프로세스를 두 명령 이름 모두에서 중지하세요.** 실행 중인 샌드박스,
`monitor`, `tui`, `mcp serve`는 데이터베이스를 열어 두고 있으며, 살아 있는 프로세스 아래에서
파일 이름을 바꾸면 그 프로세스가 쓰던 데이터가 손상됩니다.

```sh
dgx-cli sandbox stop      # `korbit sandbox stop` 명령이 있다면 그것도 함께
# 그다음 실행 중인 `monitor`, `tui`, `mcp serve`를 모두 종료합니다
```

**2. `~/.digitalx-cli`가 아직 없는지 확인하세요.** 이미 있는 디렉터리로 `mv`하면 병합되지
않고 그 **안으로 들어갑니다** — `~/.digitalx-cli/.korbit-cli`가 되어 버립니다. 둘 다 있다면
어느 쪽을 쓸지 먼저 정하고 다른 하나를 정리하세요.

```sh
ls -d ~/.digitalx-cli     # "No such file or directory"가 나와야 합니다
mv ~/.korbit-cli ~/.digitalx-cli
```

PowerShell:

```powershell
Test-Path ~\.digitalx-cli    # False여야 합니다
Move-Item ~\.korbit-cli ~\.digitalx-cli
```

**3. 데이터베이스 세 개와 각각의 동반 파일 이름을 바꾸세요.** 디렉터리 이름이 바뀌었으니
이제 CLI는 현재 파일 이름을 찾습니다. 여기서 빠뜨린 파일은 CLI가 열지 않는 파일이 됩니다.

```sh
cd ~/.digitalx-cli
for pair in "korbit-cli.db journal.db" "korbit-bot.db bot.db" "sandbox/korbit-sandbox.db sandbox/sandbox.db"; do
  set -- $pair
  for suffix in "" -wal -shm -pid .market-snapshot.json; do
    [ -e "$1$suffix" ] && mv "$1$suffix" "$2$suffix"
  done
done
```

PowerShell:

```powershell
cd ~\.digitalx-cli
$pairs = @{
  'korbit-cli.db'                = 'journal.db'
  'korbit-bot.db'                = 'bot.db'
  'sandbox\korbit-sandbox.db'    = 'sandbox\sandbox.db'
}
foreach ($from in $pairs.Keys) {
  foreach ($suffix in @('', '-wal', '-shm', '-pid', '.market-snapshot.json')) {
    if (Test-Path "$from$suffix") { Move-Item "$from$suffix" "$($pairs[$from])$suffix" }
  }
}
```

(`-pid`와 `.market-snapshot.json`은 샌드박스 데이터베이스에만 있습니다. 반복문은 없는 파일을
건너뜁니다. `-wal`과 `-shm`은 SQLite의 write-ahead log(WAL)와 공유 메모리 인덱스로, `-wal`
없이 옮긴 데이터베이스는 아직 본체에 반영되지 않은 내용을 잃습니다.)

**4. 이전 아티팩트 캐시를 지우세요.** 다시 만들 수 있는 내려받기 결과만 들어 있고, 새 위치는
다음 샌드박스 실행 때 알아서 다시 채워집니다.

| OS | 이전 캐시 |
|---|---|
| Linux | `~/.cache/korbit-cli` |
| macOS | `~/Library/Caches/korbit-cli` |
| Windows | `%LOCALAPPDATA%\korbit-cli` |

```sh
rm -rf ~/.cache/korbit-cli          # macOS에서는 ~/Library/Caches/korbit-cli
```

```powershell
Remove-Item -Recurse -Force $env:LOCALAPPDATA\korbit-cli
```

**고정된 홈은 위의 모든 것에 우선합니다.** `DIGITALX_CLI_HOME`이나 `KORBIT_CLI_HOME`이
설정되어 있으면 `~` 아래에 무엇이 있든 그 경로가 홈이며, 변수를 바꾸기 전까지는
`~/.korbit-cli`를 옮겨도 달라지는 것이 없습니다. 고정한 디렉터리 역시 **자기 basename**에
따라 같은 파일 이름 규칙을 따릅니다. `.korbit-cli`라는 이름의 디렉터리에 고정하면 이전 파일
이름을 그대로 쓰며, 이전 `korbit` 바이너리가 그 홈을 여전히 함께 쓰고 있다면 그 배치가
맞습니다.

마지막으로 확인합니다.

```sh
dgx-cli self doctor
```

## `self doctor`가 보고하는 것

**문제**(problem)는 망가진 것(0이 아닌 코드로 종료), **참고**(note)는 동작은 하지만 어떤
명령이 정리해 줄 수 있는 것(종료 코드 0)입니다. 모든 문제에는 변하지 않는 `field` 태그가
붙습니다.

| `field` | 문제 | 해결 |
|---|---|---|
| `managed` | 설치 매니페스트 없음 또는 손상 | 설치 한 줄 명령 |
| `binary` | 설치된 바이너리 없음 또는 매니페스트와 불일치 — `korbit`은 일치하는데 그 옆의 `dgx-cli`는 불일치하는 경우 포함(문서에 적힌 이름이 이 설치본이 놓은 적 없는 바이트를 실행하게 됨) | 설치 한 줄 명령. `dgx-cli`만 불일치하면 `self update` |
| `alias` | `korbit` 명령 없음, `dgx-cli`가 아닌 것을 가리킴, 또는 다른 버전의 오래된 사본 | `self update` |
| `path` | 설치 디렉터리가 `PATH`에 없음 | 설치 한 줄 명령 |
| `home` | 아래 두 상태 중 하나 | 사용자가 직접 |

두 디렉터리 이름에 관해 보고되는 상태는 **두 가지**뿐이며, 둘 다 CLI가 읽지 않는 데이터가
있다는 뜻입니다.

- **CLI 홈이 두 개 있고, 사용되지 않는 쪽에 데이터가 있음.** 어느 쪽이 사용 중인지 함께
  알려 주므로 어느 절반이 보이지 않는지 알 수 있습니다. 사용 중인 홈에 키도 설정도 저널도
  전혀 없는데 다른 쪽에는 있다면 그 사실을 분명히 말해 줍니다 — 모호한 상황이 아니라
  확실한 상황이기 때문입니다. **해결:** 필요한 것을 옮기거나 원하지 않는 디렉터리를
  제거하세요. [방법 B](#방법-b--직접-옮기기)를 따르면 됩니다.
- **사용 중인 홈에 이전 파일 이름의 데이터베이스가 있고**, 그 디렉터리 이름은
  `.korbit-cli`가 아님 — 파일은 두고 디렉터리만 이름을 바꾼 경우입니다. 어떤 파일을 어떤
  이름으로 바꿔야 하는지 하나씩 알려 줍니다. **해결:** [방법 B](#방법-b--직접-옮기기)의
  3단계. 고정된 홈이라면 변수를 `.korbit-cli`라는 이름의 디렉터리로 대신 가리키게 하면,
  이전 배치가 그대로 유지되어 바꿀 파일이 없습니다.

이전 이름에 관한 그 밖의 모든 것은 **보고하지 않습니다** — 지적 사항이 아니라 올바른
상태입니다. 이전 이름의 데이터베이스가 있는 `~/.korbit-cli`, 이전 이름의 아티팩트 캐시,
`.korbit-cli` 홈에 `journal.db`가 하나 굴러다니는 것까지 모두 그렇습니다. 그곳에서는 이전
이름이 맞는 이름이기 때문입니다.

## 바뀌지 않는 것

- **`korbit` 명령 이름.** 그 이름을 가진 설치본은 영구히 그대로 유지하며, 설치와 업데이트
  때마다 갱신되므로 뒤처지지 않습니다.
- **이 설치본이 놓지 않은 `korbit` 이름의 파일.** 무엇을 덮어쓰거나 지우기 전에 소유권이
  증명되어야 합니다 — 매니페스트에 기록되어 있거나, 매니페스트 자신의 실행 파일이거나,
  실행 중인 바이너리이거나, 그 바이트의 SHA-256이 매니페스트의 값과 일치해야 합니다.
  사용자가 직접 만든 래퍼 스크립트가 그 이름에 있다면 보고만 하고 그대로 둡니다.
- **이전 이름에 남겨 둔 설치본.** `~/.korbit-cli`, 그 안의 이전 데이터베이스 이름, 이전
  이름의 아티팩트 캐시는 영구히 읽힙니다 — 기한 없이 완전히 지원되는 상태입니다. 현재
  이름을 쓰는 것은 사용자가 선택해서 직접 수행하는 일이지, 언젠가 저절로 일어나는 일이
  아닙니다.
- **키 호환성.** 키를 저장하고, 암호화하고, 읽는 방식은 전혀 바뀌지 않습니다. 어느
  세대에서 쓴 키스토어든 양쪽 모두가 읽으며, OS 키체인 항목을 다시 만들거나 다시
  암호화하는 일은 없습니다.
- **환경 변수.** 두 표기 모두 기한 없이 인식하며, 둘 다 설정되어 있으면 `DIGITALX_CLI_*`가
  이깁니다 — 단, 그 값이 비어 있지 않을 때만입니다. 빈 `DIGITALX_CLI_*`는 설정되지 않은
  것으로 읽혀 `KORBIT_CLI_*`로 넘어가므로, 설정을 끄려면 둘 다 해제하세요.
- **고정된 홈의 디렉터리와 그 내용.** `DIGITALX_CLI_HOME`이나 `KORBIT_CLI_HOME`으로 고정한
  홈은 자기 basename이 함의하는 [파일 이름 배치](#cli-홈-안의-파일)를 영구히 유지합니다.
- **이전 릴리스 아카이브와 번들 URL.** 이미 설치된 버전이 이름으로 가져가는 것들입니다.
  이전 모든 태그의 `korbit_*` 아카이브는 언제나 있던 그 URL에서 계속 내려받을 수 있습니다.

## 결과를 JSON으로 읽기

위의 모든 결과는 산문이 아니라 필드이므로, `--json`만으로 바로 처리할 수 있습니다.

- `self update` → `layoutRepaired`(이번 실행에서 고친 명령 이름들. `checkedOnly`가 실제로
  적용됐는지 계획만 했는지 알려 줍니다)와 `next`(다음 단계). 마이그레이션 필드는 없습니다 —
  마이그레이션 자체가 없기 때문입니다.
- `self install` → `repaired`, `path`, `next`.
- `self doctor` → 망가진 것은 `problems[].field`, 동작하지만 정리할 수 있는 상태는
  `notes`, 그리고 디렉터리를 산문에서 파싱해 낼 필요가 없도록 `home`과 `legacyHome`.

## 관리자를 위한 안내

일반적인 릴리스는 아카이브 세트를 **하나만** 게시하며, 그 목록은 서명된 `checksums.txt`에
들어갑니다. 함께 올라가는 `release-manifest.json`은 플랫폼별로 어떤 아카이브를 내려받고 그
안의 바이너리 이름이 무엇인지 알려 줍니다. 설치본은 자신에게 컴파일된 이름을 가정하지 않고
릴리스에서 그 이름을 읽어 옵니다. 덕분에 이름을 바꾸더라도, 이전 이름으로 설치된 것들이
남아 있는 동안 두 번째 세트를 계속 게시할 필요 없이 아카이브 세트 하나로 끝납니다:
[`RELEASING.md`](https://github.com/digitalx-official/digitalx-cli/blob/master/RELEASING.md).
